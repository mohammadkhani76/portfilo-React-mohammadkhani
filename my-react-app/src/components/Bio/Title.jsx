const Title = ({ title,descrtiption }) => {
    return (
        <div className="bio-title">
            <h1>{title}</h1>
            <p>{descrtiption}</p>
        </div>
    );
}
export default Title;
